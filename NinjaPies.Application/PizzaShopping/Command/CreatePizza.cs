using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace NinjaPies.Application.Pizza.Command
{
    public class CreatePizza
    {
         // IPizzaRepository       

        // public override OperationResponse Execute(PizzaDTO request)

        // var pizza = Map PizzaDTO to Pizza Object

        // Calculate Cost of Pizza based on user selections
        // cost = x...
        // pizza.setCost(x);


        // pizza.CreatedBy = 
        // pizza.CreatedDate = 

        // return SuccessfulResult(pizza.Id);
    }
}
